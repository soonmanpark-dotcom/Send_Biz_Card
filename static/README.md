# static

메일에 넣을 이미지를 두는 폴더입니다.

여기에 `profile.jpg` 를 올리면 다음 주소로 제공됩니다.

    https://send-biz-card.onrender.com/static/profile.jpg

이 주소를 `config.yaml` 의 `card.photo_url` 에 넣으면 명함에 사진이 표시됩니다.
정사각형으로 잘라서 올려야 원형으로 깔끔하게 보입니다.
